# Restaurant_webpage

Run by just opening index.html in any browser
OR
Use live server


Note: if images are not visible( which is rare) make sure to check the image path in your folder where you have downloaded, 
To avoid these issues, directly download the whole repo as a zip file or clone the repo.
